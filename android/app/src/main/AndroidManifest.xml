<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- OpenGL ES 3.0 required for Bevy/wgpu -->
    <uses-feature android:glEsVersion="0x00030000" android:required="true" />

    <!-- Touch screen support -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WhirledPeas"
        android:extractNativeLibs="true"
        android:hasCode="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|density"
            android:screenOrientation="fullSensor"
            android:theme="@style/Theme.WhirledPeas.Fullscreen">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Declare that this activity uses the GameActivity native library -->
            <meta-data
                android:name="android.app.lib_name"
                android:value="whirled_peas" />
        </activity>
    </application>

</manifest>
